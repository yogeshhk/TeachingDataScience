# TODO (TeachingDataScience)

Link-only index into the active sub-project TODOs.

## Pending

- **MLCoEP final/end-sem exam question bank: check correctness with Opus 5.5.** Mid-sem is already
  done; this is the final exam (Sessions 1-20, 20 descriptive + 20 numerical, per
  `Code/mlcoep/README_AIML4ME_2026.md`'s exam-structure section). **Not in this repo, by design**:
  that same README states question banks are maintained outside it on purpose, this is a public repo
  and exam content is never committed here. **Location: `Desktop/MLCoEP_ML_FinalExam_QuestionBank.md`**
  (kept there deliberately, recreatable if lost, so not itself a big loss). Worth also placing a copy
  in Google Drive at some point since Desktop alone isn't backed up anywhere else, not blocking the
  review.

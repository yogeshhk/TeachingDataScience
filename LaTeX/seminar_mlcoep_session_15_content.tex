% Session 15: KNN
\section[KNN]{Session 15: KNN}
\input{ml_knn}
% Excluded (Sep 2026, reconfirmed): sklearn/Iris walkthrough duplicates Session 7's sklearn
% workflow (ml_intro_sklearn.tex already loads Iris and demonstrates KNeighborsClassifier).
% Still \input in full by seminar_ml_knn_content.tex (standalone ML course seminar).
%\input{ml_knn_sklearn}
% The NBA case study from that same file is unique (not covered anywhere else in this
% session) and was re-added on its own below, extracted into its own file so the excluded
% Iris section isn't duplicated for the standalone ML course, which already gets it via
% ml_knn_sklearn.tex above.
\input{ml_knn_nba_case_study}

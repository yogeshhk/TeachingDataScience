references/

# Compiled decks and build litter are never committed here (public repo policy,
# see CLAUDE.md "Compiled output policy") -- final PDFs live in the private
# Publications repo instead. images/*.pdf are source assets, not build output,
# so this only matches compiled top-level Main_* decks.
Main_*.pdf
*.aux
*.log
*.nav
*.out
*.snm
*.toc
*.vrb
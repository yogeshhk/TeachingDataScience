In this exercise you have to implement three different types of tree traversals for binary trees:

- `preorder_traversal`
- `inorder_traversal`
- `postorder_traversal`

Each function should return a list of the values in the correct order. 

name: interview
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pandas
  - jupyter
  - pip
  - pip:
    - torch
# Usage:
#   conda env create -f environment.yml
#   conda activate interview

# Code Folder Upgrade Report
**Date:** 07-04-2026  
**Scope:** `Code/` folder only  
**Reviewer:** Claude Code (claude-sonnet-4-6)

---

## Summary

Performed a deep structural review of all 47 subdirectories in `Code/`. Changes were surgical — no logic was altered, no files were deleted except empty directories and git-tracked build artifacts.

---

## Changes Made

### Structural
| Action | Target | Reason |
|--------|--------|--------|
| Deleted | `Code/opencv/` | Empty directory |
| Deleted | `Code/openwork/` | Empty directory |
| Created | `Code/reports/` | Storage for review reports |
| Created | `Code/.gitignore` | Central gitignore for all Code subdirectories |

### Critical Fixes
| Action | Target | Reason |
|--------|--------|--------|
| `git rm --cached` | `Code/google-adk/my_agent/__pycache__/` | Binary `.pyc` files were tracked in git |
| Renamed (18 files) | `Code/dl_tf2/dl_tf2_ashish_*.ipynb.ipynb` → `*.ipynb` | Double extension prevented Jupyter from opening them |

### Conda Environments Created
Replaced `requirements.txt`-only approach with `environment.yml` files for conda-based environment management. `conda env create -f environment.yml && conda activate <name>` is now the standard setup flow.

| Directory | Env Name | Notes |
|-----------|----------|-------|
| `chatbot-faqs/` | `chatbot-faqs` | |
| `chatbot-multimodal/` | `chatbot-multimodal` | |
| `omni-rag/` | `omni-rag` | |
| `parsing/` | `parsing` | |
| `docling/` | `docling` | |
| `llamaindex/` | `llamaindex` | Unpinned versions (original requirements.txt kept for reproducibility) |
| `python/` | `python-ds` | |
| `langchain/` | `langchain` | New — was missing |
| `langgraph/` | `langgraph` | New — was missing |
| `agents/` | `agents` | New — was missing |
| `ml/` | `ml` | New — was missing |
| `keras/` | `keras` | New — was missing |
| `dl_tf2/` | `dl-tf2` | New — was missing |
| `fine-tuning/` | `fine-tuning` | New — GPU recommended |
| `google-adk/` | `google-adk` | New — was missing |

### Documentation Added
New `README.md` files created for directories that had none:

| Directory | Contents |
|-----------|----------|
| `langgraph/` | Full table of notebooks, learning path, key concept explanation |
| `langchain/` | Cookbook and series breakdown, API version note |
| `ml/` | Learning path, contributor attribution, data folder note |
| `keras/` | Chapter-by-chapter breakdown (Chollet book mapping) |
| `dl_tf2/` | Series breakdown, note on deprecated Swift for TF |
| `txt2cad/` | Research reference disclaimer + file list |
| `txt2sql/` | Research reference disclaimer + file list |

Updated existing `README.md`:
- `agents/` — was essentially empty; now documents three frameworks and links to dedicated dirs

---

## Deferred / Requires Manual Action

### Security — Rotate Google API Key
`Code/google-adk/my_agent/.env` contains a real `GOOGLE_API_KEY`. The file is gitignored and not in git history, but the key should be rotated in Google Cloud Console as a precaution.

### deep_rl — Duplicate Notebooks (awaiting confirmation)
Units 1–5, 7, 8 exist in two naming variants:
- `deep_rl_class_Hugging Face Unit N_<title>.ipynb` (title-case, verbose)
- `deep_rl_class_huggingface_unitN.ipynb` (lowercase, consistent)

Recommend keeping lowercase versions and deleting the title-case ones. **Confirm before deleting.**

### ml/data/requirements.txt
`Code/ml/data/requirements.txt` contains very old pinned versions (`numpy==1.22.0`, `matplotlib==1.5.1`). Not removed — may be needed for data-generation scripts in that folder.

---

## What Was Already Good
- `chatbot-faqs/`, `chatbot-multimodal/`, `docling/`, `parsing/`, `omni-rag/` — production-quality structure with README + requirements
- `crewai/` — modern `pyproject.toml` + `uv.lock` dependency management
- `curiosily_ai_bootcamp/` — strong README with external links and learning path
- Consistent notebook naming convention (`<framework>_<source>_<topic>.ipynb`) across the whole repo

# Code/ Upgrade Report — 26 April 2026

Scope: `Code/` directory only (LaTeX/ and other top-level directories excluded).

---

## Project Summary

`Code/` contains 55 subdirectories with ~350 `.py` files and ~800+ `.ipynb` notebooks spanning classical ML, deep learning, NLP, GenAI frameworks, RAG systems, and agentic workflows. Active development concentrates in the GenAI/RAG cluster (`chatbot-multimodal`, `omni-rag`, `parsing`, `chatbot-faqs`) and agentic frameworks (`agents`, `langgraph`, `google-adk`, `agno`). Notebook-heavy directories (`ml`, `keras`, `dl_tf2`) are tutorial collections with minimal Python scripts.

---

## Changes Applied

### Step 1 — Structural Changes

| Action | Path | Reason |
|--------|------|--------|
| Deleted | `kartikeyapandey20/` | Confirmed empty directory |
| Created | `agno/README.md` | 3 runnable scripts with no docs |
| Created | `graphrag/README.md` | 2 scripts with no docs |
| Created | `sarvam/README.md` | 12 scripts with no docs |
| Created | `google-adk/README.md` | 9 scripts with no docs |
| Created | `llamaindex/README.md` | 31 files with no docs |
| Created | `pytorch/README.md` | 24 files with no docs |
| Created | `spacy/README.md` | 13 scripts with no docs |
| Created | `gnn/README.md` | 26 scripts with no docs |

### Step 2 — Critical Bug Fixes

| File | Line | Fix |
|------|------|-----|
| `chatbot-multimodal/docling_parsing.py` | 125 | Removed `self.text_model.to(self.device)` — `ChatGroq` is not a PyTorch module and has no `.to()` method; calling it crashes `DoclingParser.__init__()` |
| `chatbot-multimodal/langchain_rag.py` | 364–382 | Replaced `processed_context_runnable.invoke(x['context'], x['question'])` (2-arg invoke, crashes at runtime) with a direct call `self._process_retrieved_documents(x['context'], x['question'])` inside the LCEL lambda; also removed the now-unused `RunnableLambda` wrapper |
| `google-adk/multi_agent.py` | 7 | Replaced `pass` stub in `web_search()` with a descriptive return string so the tool doesn't return `None` silently |
| `graphrag/graphrag_langchain_spicejet.py` | 55–58 | Replaced deprecated `chain.run()` (removed in LangChain 0.3) with `chain.invoke()`; added `print()` around results |

### Step 3 — Deprecation Fixes

| File | Change |
|------|--------|
| `chatbot-multimodal/ragas_rag.py:67` | `.get_relevant_documents()` → `.invoke()` (removed in LangChain 0.2+) |
| `chatbot-multimodal/langchain_rag.py:31–33` | `langchain_community.embeddings.HuggingFaceEmbeddings` and `langchain_community.llms.HuggingFacePipeline` → `langchain_huggingface` package |
| `omni-rag/evaluate.py:49` | `f"Context: {context_str}"` where `context_str` is `List[str]` → `f"Context: {' '.join(context_str)}"` so the LLM receives plain text, not a Python list representation |

### Step 4 — Medium Improvements

| File | Change |
|------|--------|
| `parsing/llm_parsing_groq.py:9` | Default model `"gemma-7b-it"` (retired) → `"gemma2-9b-it"` |
| `chatbot-multimodal/docling_parsing.py:15–16` | Removed scaffolding comments `# Ensure this is present` and `# ADD THIS IMPORT` |
| `agno/rag_agent.py` | Replaced empty file with a TODO stub comment |

---

## Deferred / Not Changed

| Item | Reason deferred |
|------|-----------------|
| `graphrag/graphrag_langchain_spicejet.py:44` — `graph._graph.add_edge()` private API | LangChain's `NetworkxEntityGraph` does not expose a public `add_edge()`; private access is the only current option. Left with a note in README. |
| `omni-rag/evaluate.py:18` — model `"llama3-70b-8192"` | Still listed in Groq's model catalogue as of April 2026; no change needed. |
| Duplicate RAG pattern across `chatbot-multimodal/`, `omni-rag/`, `parsing/` | Each serves a distinct teaching objective (LangChain vs LlamaIndex vs raw Groq); consolidation would reduce instructional clarity. |
| 22 remaining directories without README | `ml/`, `dl_tf2/`, `keras/` are notebook tutorial collections — a top-level README would add less value; deferred for a future pass. |

---

## Directories Still Without README (lower priority)

`activeloop`, `animesh1012`, `amd`, `curiosily_ai_bootcamp` (has one), `dbpedia`, `deep_rl`, `dl_curiousily`, `dnlp`, `fine-tuning` (has one), `gcp_notebooks`, `gemma`, `langchain` (has one), `llama`, `ludwig` (has one), `mahamarathi`, `math`, `meta_bAbi_tasks`, `ml` (has one), `nlp`, `nvidia`, `opencode`, `orgpedia`, `pandas`, `prodramp`, `python`, `vizuara`

---

## Stats

- Files modified: 7
- Files created: 9 (README) + 1 (report) = 10
- Directories deleted: 1
- Confirmed bugs fixed: 4 critical + 3 high-priority deprecations + 2 medium

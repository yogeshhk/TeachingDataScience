name: google-adk
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - google-adk>=0.1.0
    - google-generativeai>=0.7.0
    - python-dotenv
# Usage:
#   conda env create -f environment.yml
#   conda activate google-adk
#   cp my_agent/.env.example my_agent/.env  # then fill in GOOGLE_API_KEY
